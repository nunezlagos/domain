status: implemented
created: 2026-06-06
implemented: 2026-06-11
archive_reason: |
  Cierre formal: el alcance original de la HU (5 tablas Tier-1 + helper
  txctx + tests + roles) se completo en su momento. Lo que quedaba
  pendiente (extender RLS a observations/sessions + wireup HTTP) se
  migro a la HU-25.14 (issue-25.14-rls-http-wiring) que ya esta
  implemented. La 25.5 queda cerrada al 100%.

  El duplicado issue-25.5-rls-sensitive-tables (creado el 2026-06-07
  con scope "las 12 tablas Tier-1") queda archivado y la version
  canonica es la "selective" (5 tablas reales existentes + helper + tests).
  Si en el futuro se agregan tablas a las 12 Tier-1 que quedaron fuera
  (knowledge, prompts, skills, agents, flows, crons, webhooks, audit_diffs,
  api_key_scopes), abrir nuevas HUs siguiendo el patron de 25.14.
notes: |
  Migration 000028 (RLS en 5 tablas Tier-1: secrets, audit_log,
  otp_codes, activity_log, api_keys) + helper internal/store/txctx +
  tests de aislamiento (2 orgs, cross-org leak blocked, no SET LOCAL
  -> 0 rows, INSERT wrong org rejected) + 9 escenarios + 3 sabotajes
  todo verde desde 2026-06-10. Ver tests en txctx_integration_test.go.

  Pendiente migrado a HU-25.14 (now implemented).
