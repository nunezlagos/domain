status: implemented
created: 2026-06-07
implemented: 2026-06-10
archived: ~
notes: |
  Ollama Provider (Complete + Stream) en internal/llm/ollama/provider.go.
  Embedder (nomic-embed-text) con padding a 1536 dims.
  Config env: DOMAIN_OLLAMA_URL (+ legacy DOMAIN_OLLAMA_HOST),
  DOMAIN_OLLAMA_AUTO_PULL opt-in (404 model → /api/pull + retry único).
  Timeout default 120s. Streaming via NDJSON.
  10 unit tests httptest (basic, stream, 404, refused, custom URL,
  env overrides, ctx timeout, auto-pull x2, sabotaje JSON inválido).
