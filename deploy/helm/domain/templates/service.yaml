apiVersion: v1
kind: Service
metadata:
  name: {{ include "domain.fullname" . }}
  labels:
    {{- include "domain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domain.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "domain.fullname" . }}-metrics
  labels:
    {{- include "domain.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "domain.selectorLabels" . | nindent 4 }}
