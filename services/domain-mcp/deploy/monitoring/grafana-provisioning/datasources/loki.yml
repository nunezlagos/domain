# DOMAINSERV-81: datasource Loki (logs). Prometheus sigue siendo el default.
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
