status: partially_implemented
created: 2026-06-06
implemented: 2026-06-07
archived: ~
notes: |
  sdks/python/ con paquete domain_sdk:
  - pyproject.toml: hatchling, deps httpx + pydantic, opcional dev (pytest +
    respx + ruff + mypy strict)
  - DomainClient async con context manager + httpx.AsyncClient interno
  - Errores tipados: AuthError/NotFoundError/ValidationError/ConflictError/
    RateLimitError/QuotaExceededError según status code + body.error
  - Resources: organizations, projects, observations, sessions, search,
    skills, agents (con run + logs), flows, knowledge
  - Pydantic models: Organization, Project, Observation, Session,
    SearchResult, AgentRunResult

  Tests con respx mocking (5 escenarios):
  - create_project happy path
  - observations.save
  - 401 → AuthError
  - 404 → NotFoundError
  - 409 → ConflictError con code preservado
  - 402 → QuotaExceededError
  - sin api_key → AuthError al constructor

  README.md con quick start + table de config + manejo de errores + lista
  resources.

  Pending:
  - Streaming responses (SSE para /agents/{id}/run/stream)
  - Pagination cursor automática para list endpoints
  - Retry exponential backoff en 5xx/connection errors
  - Publish a PyPI vía CI tag-based (HU-22.0)
  - Sync mode opcional (algunas integraciones requieren sync)
  - OpenAPI spec generation y mypy code-gen
