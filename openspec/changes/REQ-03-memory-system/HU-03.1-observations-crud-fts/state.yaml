status: partially_implemented
created: 2026-06-07
implemented: 2026-06-07
archived: ~
notes: |
  internal/service/observation.Service:
  - Save(SaveInput): valida content + project, genera embedding (Embedder
    inyectado), INSERT observations con vector cast pgvector, audit log.
  - Get(id): SELECT WHERE deleted_at IS NULL
  - List(projectID, limit): orden created_at DESC, default 50, cap 200
  - SoftDelete(id): UPDATE deleted_at = NOW()
  - SearchHybrid(orgID, query, limit): CTE con BM25 (ts_rank) + cosine
    (<=> pgvector) + Reciprocal Rank Fusion (k=60). Si Embedder es
    NopEmbedder (vector zero), degrada a tsvector-only.

  internal/llm.Embedder interface:
  - NopEmbedder: vector zero (dev/sin API key)
  - FakeEmbedder: vector determinístico hash-based unit-norm (tests)
  - OpenAI/Anthropic impls: pending HU-12.2/3

  Tests integration (10 escenarios + 1 sabotaje):
  - Save persiste + embedding generado
  - Empty content → ErrContentRequired
  - Get/List rounds correctos
  - SoftDelete idempotente + Get retorna ErrNotFound
  - SearchHybrid encuentra top match con pgvector query
  - SearchHybrid degrada a tsvector-only con NopEmbedder
  - Empty query → nil sin error
  - sabotaje cross-org search no leak (organization_id filter en SQL)

  Pending HU-03.2/3/4/5/6/7:
  - sessions lifecycle
  - prompts storage (distinto de observations)
  - knowledge documents (chunks separados)
  - context timeline (cross-entity feed)
  - dedup + PII privacy
  - cross-project global search
