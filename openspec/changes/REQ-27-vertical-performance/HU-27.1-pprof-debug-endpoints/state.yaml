status: partially_implemented
created: 2026-06-07
implemented: 2026-06-08
archived: ~
notes: |
  ## Implementado (2026-06-08)

  ### Audit + Metrics
  - Config.AuditRecorder opcional — registra "debug.pprof.accessed" via audit.Recorder
  - Config.Metrics opcional — expone domain_debug_pprof_accessed_total counter
  - instrumentPPROF wrapper que mete audit + metrics en todos los endpoints pprof
  - PprofAccessTotal counter agregado a metrics.Registry
  - Wire en cmd/domain/main.go: pasa recorder + metricsReg al debug config

  ### Tests
  - TestInstrumentPPROF_CallsNextHandler — verifica que el handler next se ejecuta
  - TestInstrumentPPROF_NopRecorderDoesNotPanic — verifica que nil audit no paniquea
  - Tests existentes de basic auth + defaults siguen pasando

  ## Pending (aún no implementado)
  - Helm chart: Service interno ClusterIP en puerto 6060 (Fase 9)
  - Audit y metrics se registran pero no hay label por endpoint específico
