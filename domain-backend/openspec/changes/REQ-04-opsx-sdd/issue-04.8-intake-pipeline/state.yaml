status: implemented
created: 2026-06-09
started: 2026-06-09
archived: ~
notes: |
  Implementado: tablas intake_payloads + intake_attachments con CHECK
  constraints, state machine completa (received → classifying → classified →
  deduping → structuring → pending_review → approved | rejected → committed |
  failed), Service con Submit, UpdateClassification, MarkPendingReview,
  Approve, Reject, LinkCommitted, Get, ListPending. Audit hooks integrados.
  MCP tools: domain_intake_{submit,get,list_pending,approve,reject} —
  registradas en MCP server y wireadas en domain-mcp.
  Pendiente: workers async, LLM classify skill, pgvector dedup,
  adapters email/webhook/slack/sheet, HTTP endpoints.
