status: implemented
created: 2026-06-07
implemented: 2026-06-07
archived: ~
notes: |
  Migration 000031_observations_content_hash:
  - Columna observations.content_hash BYTEA (32 bytes SHA-256)
  - UNIQUE PARTIAL index (project_id, observation_type, content_hash)
    WHERE content_hash IS NOT NULL AND deleted_at IS NULL
  - Defense-in-depth: la app valida; DB rechaza si la bypasean

  internal/memory/privacy:
  - Strip(content) → (clean, redactedCount): regex (?s)<private>.*?</private>
  - HasPrivateBlocks(content), NormalizeWhitespace(content)
  - Tests: 6 escenarios + 1 sabotaje (tag malformado sin cierre NO se quita)

  internal/memory/dedup:
  - Hash(FingerprintInput): SHA-256 sobre concat con NUL separator de
    {ProjectID, lower(ObservationType), normalize(Title), normalize(Content)}
  - normalize() = lowercase + collapse whitespace + trim
  - 6 tests determinismo + casos donde hash debe diferir

  Integración en internal/service/observation.Save:
  1. privacy.Strip(content) — si redactedCount>0 graba en metadata
  2. ErrContentRequired si tras stripping queda vacío
  3. dedup.Hash computado y pasado al INSERT
  4. Si UNIQUE PARTIAL constraint dispara → ErrDuplicate
  5. Audit incluye redacted count

  Tests integration nuevos (5 escenarios + 1 sabotaje):
  - Dedup detecta duplicado exacto
  - Dedup normaliza whitespace/caso (Fix Login == fix  login)
  - Dedup scoped por type (mismo content distinct type permitido)
  - Strip remueve <private> + metadata.privacy_redacted_blocks
  - Sabotaje DB-level: UNIQUE constraint rechaza INSERT directo aunque
    la app lo permita (defense in depth verificado)

  Rolling window N=1000 de la spec: NO implementado. Decisión arquitectónica:
  unique constraint es PERMANENT (más simple, más seguro). Si el usuario
  quiere recrear una observation idéntica vieja debe usar SoftDelete primero.
  Pending HU futura si se necesita "stale dedup window" real.

  Pending:
  - Hash backfill para observations existentes pre-migración 31
  - PII detection automática sin marcado <private> — HU-25.11 anonymization
  - Compresión content > 100KB
