# Como usuario, al iniciar un flow SDD elijo auto vs human-in-the-loop en vez de caer en auto por omisión.

## Why
exec_mode default silencioso = auto; el usuario nunca ve la opción. La pausa server-side ya existe pero no se elige el modo.

## Scope
Ver REQ-55 proposal + spec.md de este issue.
