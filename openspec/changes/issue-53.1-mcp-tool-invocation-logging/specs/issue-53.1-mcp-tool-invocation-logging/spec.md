# Como operator del MCP, cada tool invocation deja un registro consultable (slog + tabla mcp_tool_invocations) con tool_name, principal_id, status y duration_ms para diagnosticar problemas y entender que herramientas se usan en produccion.

