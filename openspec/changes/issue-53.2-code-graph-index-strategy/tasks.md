# Como operator del MCP, el grafo de codigo de cada proyecto se construye incrementalmente al indexar el repo, expone summaries via FTS + embeddings opcionales + AST cache, y soporta query expansion para que el agente navegue codigo de proyectos grandes en segundos sin reparsear. — Tasks

