status: partially_implemented
created: 2026-06-07
implemented: 2026-06-07
archived: ~
notes: |
  internal/auth/apikey: Generate (plaintext+prefix+bcrypt hash cost 12),
  GeneratePlaintext (rápido sin bcrypt), Verify (constant-time bcrypt compare),
  ParsePrefix, IsAPIKeyFormat. Format: domk_{live|test|dev}_{base64url_32bytes}.
  PrefixLen=16. Middleware HTTP con Authorization: Bearer header + allowlist
  paths + anti-enumeration body uniforme + Principal propagado via context.
  Tests: 11 unit apikey + 6 unit middleware + 2 sabotaje (bcrypt cost min,
  1000 keys uniqueness, 401 body uniforme).
  Pending: PG resolver impl (store/pg/apikey) + endpoints CRUD admin.
