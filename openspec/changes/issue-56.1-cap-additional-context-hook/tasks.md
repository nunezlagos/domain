# El hook SessionStart inyecta un additionalContext acotado (bajo un cap de bytes configurable) y resumido, de modo que el agente arranca cada sesión sin saturar su ventana de contexto, y usa domain_context_snapshot para re-hidratación compacta tras compactación. — Tasks

