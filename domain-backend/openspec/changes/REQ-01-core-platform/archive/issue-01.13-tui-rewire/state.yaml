status: implemented
created: 2026-06-11
implemented: 2026-06-11
archived: ~
notes: |
  HU-01.13 implementado en 4 commits sobre feat/req-25.5-tui-rewire:

  1. a529e09 feat(01.13): ensureLocalEnv en install_cli.go
  2. d66a3c7 feat(01.13): TUI install con widgets selectables [X] [-]
     y error propagation con contexto (commits 2+3/3)
  3. 2525abf fix(01.13): loadEnvFile para que .env sea usable por config.Load()
  4. (este) archive + state.yaml

  Logros:
  - Auto-.env: ensureLocalEnvFile() copia .env.example si .env no
    existe. Si falta .env.example, error claro 'are you in the
    domain project root? (try: cd ~/.local/share/domain)'.
  - loadEnvFile(): parsea KEY=VALUE y los setea en os.Setenv, asi
    config.Load() los ve sin depender de godotenv.
  - Widgets selectables: [X] / [ ] / [-] (este para disabled).
    Number keys 1-9 jump. Enter select. Esc cancel.
  - Error propagation: el sub-process stderr se captura (limited
    a 4KB) y se muestra al user. Antes era opaco 'exit status 1',
    ahora muestra el error real.
  - 3 prompts visuales: mode, init, opencode. baseURL sigue como
    text input (futuro: textinput component).

  Smoke test end-to-end verificado:
  $ domain install --mode local --non-interactive
  [1/5] Detecting state    ok
  [2/5] Backing up configs ok (2 backed up, 1 skipped)
  [3/5] Bootstrap .env     ok (.env present and loaded)
  [4/5] Applying migrations ok (schema up to date)
  [5/5] Running seeders    ok (all catalogs at target version)

  Tests: 100 paquetes OK, 0 FAIL. Subio de 99 a 100 con
  internal/tui/selectable.

  Pendiente: archivar (se hace en este commit).
