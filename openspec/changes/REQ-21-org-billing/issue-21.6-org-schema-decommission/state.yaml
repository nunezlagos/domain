status: in_progress
created: 2026-06-17
implemented: ~
archived: ~
notes: |
  Decommission destructivo del schema multi-tenant. Depende de issue-21.5 (surface ya
  colapsado). Ejecución por fases A→D, cada una deployable. Fase C (DROP COLUMN/TABLE) es
  irreversible: requiere backup verificado + dry-run en staging. Esfuerzo estimado: 1-2 semanas.

  FASE B EJECUTADA (2026-06-18, commit bcc5196, ticket DOMAINSERV-3):
    5 satélites desacoplados per-consumer con migraciones reversibles (sin DROP COLUMN/TABLE):
    - 000135 cost_alerts_sent_org_nullable: org_id nullable + UNIQUE(alert_date) (era UNIQUE(org, date)).
    - 000136 org_cost_alert_thresholds_pk_swap: PK(org_id) → PK(id BIGSERIAL) + UNIQUE(org_id).
    - 000137 org_flow_config_pk_swap: mismo patrón (3 consumers: usage, flow, recovery).
    - 000138 usage_counters_pk_swap: PK compuesta (org_id, period_start) → PK(id BIGSERIAL) + UNIQUE(period_start).
    - 000139 org_enrollment_tokens_drop_org_fk: drop FK a organizations(id) + UNIQUE INDEX
      partial (org_id) WHERE revoked_at IS NULL → UNIQUE INDEX partial ((TRUE)) WHERE revoked_at IS NULL.
    4 integration tests nuevos (//go:build integration, testcontainers pgvector):
    - threshold_checker_integration_test.go: cost_alerts_sent + org_cost_alert_thresholds.
    - budget_integration_test.go: org_flow_config + cache invalidation.
    - usage_integration_test.go: usage_counters + plans.
    - service_integration_test.go: enrollment.Service (Rotate/Enroll/Revoke).
    Total: +768 lines, 14 archivos. Consumers Go verificados sin organization_id en queries/inserts.
    invitations ya removida por issue-21.5 (no requiere acción acá).
    organizations (root) queda intacta para Fase C (DROP TABLE final).
    PENDIENTE: Fase D (SDK + seeds + docs) y Fase C (DROP COLUMN/TABLE con backup+dry-run local).

  FASE D EJECUTADA (2026-06-18, commit 3ce3f94):
    SDK go/python/typescript podados — Organization model + organization_id field removidos
    del wire format. organizations.go (Go), OrganizationsResource (Python/TS) eliminados.
    tests SDK actualizados (fixtures sin organization_id).
    Docs:
      - docs/db/rls.md reescrito para reflejar estado post-Fase-A.
      - docs/single-org.md NUEVO: fuente-de-verdad del estado single-org.
    Pendiente seeds/fixtures/tests (~189 refs con organization_id): la columna sigue
    existiendo en DB hasta Fase C; los tests siguen funcionando con ella. Cleanup
    simbólico se hace como parte de Fase C (cuando la columna ya no exista).

  FASE C LISTA PARA EJECUTAR (2026-06-18, commit 106e021):
    4 migraciones destructivas (000140-000143) + script deploy + helper dry-run +
    runbook completo. NO se ejecuta automáticamente — el operador corre
    scripts/req-21.6-fase-c-deploy.sh después de leer docs/runbooks/req-21.6-fase-c.md.
    Pre-requisitos: pgBackRest backup reciente (<24h) + binario con Fase B+C deployado.
    Pasos: pg_dump → dry-run testcontainers con diff de filas → apply en prod →
    post-count → aborta si extra rows. Rollback quirúrgico (.down.sql) o completo
    (pgBackRest restore). Migraciones:
    - 000140: drop FKs hacia organizations (no destructivo de datos)
    - 000141: drop organization_id en 5 satélites Fase B
    - 000142: drop organization_id en ~49 tablas restantes
    - 000143: drop current_org_id() + trigger + organizations table

  RESUMEN FINAL al 2026-06-18 (3 commits para cerrar el requerimiento):
    - bcc5196 (Fase B): 14 archivos, +768 lines. Per-consumer cleanup de 5 satélites.
    - 3ce3f94 (Fase D): 15 archivos, +227/-184 lines. SDKs podados + docs.
    - 106e021 (Fase C ready): 11 archivos, +828 lines. Migraciones + script + runbook.
    Total: 40 archivos tocados, ~1823 insertions / ~184 deletions.
    PENDIENTE EJECUCIÓN: Fase C requiere operador (script deploy + dry-run local).
    Una vez ejecutada y verificada: status → implemented, ticket DOMAINSERV-3 → done,
    REQ-21 → archived.

  FASE A EJECUTADA (2026-06-17, decisión del operador "ejecutar solo Fase A"):
    - Migración 000132_disable_org_rls: DISABLE ROW LEVEL SECURITY en 19 tablas org-scoped
      (no otp_codes). Reversible (down = ENABLE+FORCE; policies no se dropearon).
    - sd-001 (quitar GUC org del middleware) DIFERIDO a Fase B (hot path, sin beneficio con
      RLS off, y current_user_id sigue requerido por otp RLS).
    - go build verde, go vet default sin issues. Deploy diferido (decisión operador).
  CHECKPOINT WIP (2026-06-17, workflow org-decommission run wf_2a64adba-2d9, PAUSADO):
    Fase B mayormente ejecutada por workflow multi-agente. Estado del working tree:
      - internal/service/org REMOVIDO completo (entidad org eliminada).
      - org_delete_log dropeado (migración 000133, commiteada antes del workflow).
      - organization_id removido de queries SQL + campos de struct en ~servicios/handlers/mcp/runner.
      - handler/org.go: addMemberWithKey + listMembers ahora globales (sin organization_id);
        INSERT INTO users ya NO incluye organization_id.
      - Firmas con param orgID se dejaron (sin usar, build verde — limpieza diferida).
      - go build ./... VERDE, go vet ./... VERDE.
      - Quedan ~138 archivos .go (no-test) con refs org (params sin usar, ctxkeys, middleware, comentarios).
    ⚠️ NO COMPLETADO / NO DEPLOYABLE:
      - NO se creó la migración 000134 (Fase C): columnas organization_id + tablas org
        (organizations, plans, usage_counters, invitations, org_*) SIGUEN en la DB.
      - INCONSISTENCIA código/schema: users.organization_id es NOT NULL pero el código ya
        no lo provee → INSERT de users fallaría a runtime. Hay que correr Fase C (drop columnas)
        ANTES de deployar.
      - Fase D (tests _test.go bajo -tags=integration, SDK, seeds, docs) NO ejecutada.
    REANUDAR: Workflow({scriptPath: ".../org-decommission-wf_2a64adba-2d9.js", resumeFromRunId: "wf_2a64adba-2d9"})
      continúa desde Fase Migration (los agentes de strip ya hechos vuelven cacheados).
    Commit del checkpoint: ver git log (mensaje "WIP(req-21.6)").
  INTENTO DE CIERRE (workflow org-decommission-finish, run wf_f4bac677-187) — REVERTIDO:
    Se intentó completar strip + migración 000134 (DO block drop organization_id de TODAS
    las base tables) + verificación por integration tests. El verify adversarial encontró
    un BREAK BLOQUEANTE y se reverteó al checkpoint 5b4ad8d (no se commiteó nada roto).
    LANDMINES confirmados (para el próximo intento):
      1. internal/auth/apikey/store.go corre SQL REAL contra api_keys.organization_id en
         Issue/List/Rotate/Resolve. Resolve es el PATH del middleware de auth. Dropear esa
         columna rompe TODA la auth por API key con SQLSTATE 42703, EN SILENCIO: el paquete
         NO tiene tests y las queries son strings (build no las chequea). → Arreglar apikey
         PRIMERO, y agregarle tests antes de tocar el schema.
      2. Un DROP COLUMN organization_id indiscriminado (DO block sobre information_schema) es
         demasiado agresivo: rompe api_keys y las satélites donde org_id es parte de PK/identidad
         (usage_counters PK (org_id, period), cost_alerts UNIQUE(org_id,date), enrollment).
      3. Las 8 tablas org (organizations + invitations + usage_counters + plans + org_flow_config
         + org_cost_alert_thresholds + cost_alerts_sent + org_enrollment_tokens) tienen consumidores
         Go VIVOS (usage/lifecycle/billing/enrollment/handler/SDK). Cada uno necesita refactor
         per-consumer antes de dropear su tabla.
      4. ~33 refs restantes incluyen JSON struct tags inertes (benignos) — no confundir con SQL.
    CONCLUSIÓN: el drop total no es seguro de forma 100% automatizada (sin red de tests en rutas
    críticas como apikey, y SQL en strings que el compilador no ve). Hacer incremental por-consumidor
    con tests, empezando por apikey/store.go.
  PASO 1 HECHO (2026-06-17, commit f8f87dd) — apikey desacoplado de api_keys.organization_id:
    - store.go: Issue/Rotate dejan de escribir organization_id; Resolve/List derivan el org
      del user (users.organization_id vía JOIN). La columna queda huérfana → droppable.
    - El org del Principal sigue fluyendo (desde users) → downstream intacto (no se rompió el
      flujo apikey→ctxkeys→services).
    - Migración 000134: api_keys.organization_id → nullable (reversible).
    - NUEVO store_integration_test.go: ruta de AUTH (Issue→Resolve), List, Rotate, Revoke.
    - build+vet verdes; integration apikey PASA (15s); migrate roundtrip PASA.
  PENDIENTE: refactor per-consumer de las 8 satélites (billing/usage/cost-alerts/flow-config/
    enrollment + organizations) volviéndolas globales, cada una con sus integration tests;
    luego migración final drop columnas/tablas (explícita, NO DO block ciego); Fase D
    (tests+SDK+seeds+docs); limpieza de params orgID muertos.
