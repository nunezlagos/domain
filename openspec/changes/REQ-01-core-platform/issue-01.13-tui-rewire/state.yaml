status: proposed
created: 2026-06-11
implemented: ~
archived: ~
notes: |
  HU-01.13 — TUI rewire: auto-.env + error propagation con contexto.

  Trigger: user corrio 'domain' TUI, fallo con 'Install failed: exit
  status 1' porque faltaba .env. El sub-process 'domain install' fallo
  en config validation pero el error real (DOMAIN_DATABASE_URL required)
  se perdio en el wrap de exec.Command.

  3 commits planeados:
  1. ensureLocalEnv en install_cli.go (auto-copia .env.example)
  2. error propagation en TUI install runner (capturar stderr)
  3. smoke test + state.yaml + archive

  Out of scope: eliminar el sub-process completamente. Queda para
  HU-01.14 (mover cmd/domain/install_cli.go a internal/installer/cli.go).
