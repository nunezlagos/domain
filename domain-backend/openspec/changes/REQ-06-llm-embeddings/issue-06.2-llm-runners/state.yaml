status: implemented
created: 2026-06-07
implemented: 2026-06-10
archived: ~
notes: |
  Runners Complete + Stream: openai, anthropic, google (Gemini v1beta
  generateContent + streamGenerateContent SSE), ollama.
  Resiliencia componible (2026-06-10):
  - llm/retry: exponential backoff solo en errores transient
    (429/5xx/network/timeout); auth/4xx fallan inmediato.
  - llm/ratelimit: semáforo de concurrencia; streams retienen slot
    hasta cierre del canal.
  Stack en cmd/domain: circuitbreaker(ratelimit(retry(provider))),
  maxConcurrent via DOMAIN_LLM_MAX_CONCURRENT (default 8).
  Google se registra con DOMAIN_GOOGLE_KEY.
  Tests: 6 por provider con httptest + 6 retry + 4 ratelimit.
