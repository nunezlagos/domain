Domain {{ .Chart.AppVersion }} desplegado.

Para verificar el deployment:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Health check:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "domain.fullname" . }} 8000:{{ .Values.service.port }}
  curl http://localhost:8000/health

Logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --tail=50 -f

{{- if .Values.ingress.enabled }}

API expuesta vía Ingress:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}/api/v1/health
{{- end }}
{{- else }}

Para exponer la API agrega un Ingress:
  helm upgrade {{ .Release.Name }} domain/domain --set ingress.enabled=true
{{- end }}

Próximos pasos:
1. Crear org + user inicial:
   curl -X POST http://localhost:8000/api/v1/auth/request-otp \
     -d '{"identifier":"admin@your-org.com"}'

2. Verificar OTP y obtener API key:
   curl -X POST http://localhost:8000/api/v1/auth/verify-otp \
     -d '{"identifier":"admin@your-org.com","code":"123456"}'
