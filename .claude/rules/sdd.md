Siempre que vayas a implementar un cambio:
1. Buscá la HU correspondiente en `openspec/changes/`
2. Leé `hu.md` (Gherkin), `design.md` (ADR), `tasks.md` (tasks)
3. Seguí TDD: test → implementación mínima → refactor → sabotaje
4. Si encontrás un gap en los requirements, preguntá antes de codear
