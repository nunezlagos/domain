status: implemented
created: 2026-06-12
implemented: 2026-06-13
slug: quota-snapshot-dashboard-ready
issue: 33.4
parent_req: REQ-33-saas-protection
effort: S
priority: media
notes: >
  Read-only endpoints para el dashboard futuro. Filtro por
  org SIEMPRE desde principal. Phase 1 implementada con
  queries directas (GROUP BY + generate_series para gap-fill);
  matview deferida hasta que la perf bita. observations va
  por txctx.WithOrgTx (RLS); resto filtra org_id en WHERE.
  rate_limit_per_minute expuesto desde default constant
  (no hay tabla de config aún).
