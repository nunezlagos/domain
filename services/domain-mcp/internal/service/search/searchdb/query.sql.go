// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: query.sql

package searchdb

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const searchKnowledgeDocs = `-- name: SearchKnowledgeDocs :many
SELECT kd.id, kd.title, kd.body, kd.project_id, kd.created_at,
       ts_rank(kd.body_tsv, qry)::float8 AS score
FROM knowledge_docs kd, plainto_tsquery('spanish', $1) AS qry
WHERE kd.deleted_at IS NULL AND kd.body_tsv @@ qry
  AND ($2::uuid[] IS NULL OR kd.project_id = ANY($2::uuid[]))
  AND ($3::timestamptz IS NULL OR kd.created_at >= $3::timestamptz)
  AND ($4::timestamptz IS NULL OR kd.created_at < $4::timestamptz)
ORDER BY score DESC
LIMIT $5
`

type SearchKnowledgeDocsParams struct {
	Query       string             `json:"query"`
	ProjectIds  []uuid.UUID        `json:"project_ids"`
	DateFrom    pgtype.Timestamptz `json:"date_from"`
	DateTo      pgtype.Timestamptz `json:"date_to"`
	ResultLimit int32              `json:"result_limit"`
}

type SearchKnowledgeDocsRow struct {
	ID        uuid.UUID `json:"id"`
	Title     string    `json:"title"`
	Body      string    `json:"body"`
	ProjectID uuid.UUID `json:"project_id"`
	CreatedAt time.Time `json:"created_at"`
	Score     float64   `json:"score"`
}

func (q *Queries) SearchKnowledgeDocs(ctx context.Context, arg SearchKnowledgeDocsParams) ([]SearchKnowledgeDocsRow, error) {
	rows, err := q.db.Query(ctx, searchKnowledgeDocs,
		arg.Query,
		arg.ProjectIds,
		arg.DateFrom,
		arg.DateTo,
		arg.ResultLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SearchKnowledgeDocsRow
	for rows.Next() {
		var i SearchKnowledgeDocsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.Body,
			&i.ProjectID,
			&i.CreatedAt,
			&i.Score,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchObservations = `-- name: SearchObservations :many
SELECT o.id, o.observation_type, o.content, o.tags, o.project_id, o.created_at,
       ts_rank(o.content_tsv, qry)::float8 AS score
FROM knowledge_observations o, plainto_tsquery('spanish', $1) AS qry
WHERE o.deleted_at IS NULL AND o.content_tsv @@ qry
  AND ($2::uuid[] IS NULL OR o.project_id = ANY($2::uuid[]))
  AND ($3::text[] IS NULL OR o.tags @> $3::text[])
  AND ($4::timestamptz IS NULL OR o.created_at >= $4::timestamptz)
  AND ($5::timestamptz IS NULL OR o.created_at < $5::timestamptz)
ORDER BY score DESC
LIMIT $6
`

type SearchObservationsParams struct {
	Query       string             `json:"query"`
	ProjectIds  []uuid.UUID        `json:"project_ids"`
	Tags        []string           `json:"tags"`
	DateFrom    pgtype.Timestamptz `json:"date_from"`
	DateTo      pgtype.Timestamptz `json:"date_to"`
	ResultLimit int32              `json:"result_limit"`
}

type SearchObservationsRow struct {
	ID              uuid.UUID `json:"id"`
	ObservationType string    `json:"observation_type"`
	Content         string    `json:"content"`
	Tags            []string  `json:"tags"`
	ProjectID       uuid.UUID `json:"project_id"`
	CreatedAt       time.Time `json:"created_at"`
	Score           float64   `json:"score"`
}

func (q *Queries) SearchObservations(ctx context.Context, arg SearchObservationsParams) ([]SearchObservationsRow, error) {
	rows, err := q.db.Query(ctx, searchObservations,
		arg.Query,
		arg.ProjectIds,
		arg.Tags,
		arg.DateFrom,
		arg.DateTo,
		arg.ResultLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SearchObservationsRow
	for rows.Next() {
		var i SearchObservationsRow
		if err := rows.Scan(
			&i.ID,
			&i.ObservationType,
			&i.Content,
			&i.Tags,
			&i.ProjectID,
			&i.CreatedAt,
			&i.Score,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchPrompts = `-- name: SearchPrompts :many
SELECT p.id, p.slug, p.body, p.tags, p.project_id, p.created_at,
       ts_rank(p.body_tsv, qry)::float8 AS score
FROM prompts p, plainto_tsquery('spanish', $1) AS qry
WHERE p.deleted_at IS NULL AND p.body_tsv @@ qry
  AND ($2::uuid[] IS NULL OR p.project_id = ANY($2::uuid[]))
  AND ($3::timestamptz IS NULL OR p.created_at >= $3::timestamptz)
  AND ($4::timestamptz IS NULL OR p.created_at < $4::timestamptz)
ORDER BY score DESC
LIMIT $5
`

type SearchPromptsParams struct {
	Query       string             `json:"query"`
	ProjectIds  []uuid.UUID        `json:"project_ids"`
	DateFrom    pgtype.Timestamptz `json:"date_from"`
	DateTo      pgtype.Timestamptz `json:"date_to"`
	ResultLimit int32              `json:"result_limit"`
}

type SearchPromptsRow struct {
	ID        uuid.UUID  `json:"id"`
	Slug      string     `json:"slug"`
	Body      string     `json:"body"`
	Tags      []string   `json:"tags"`
	ProjectID *uuid.UUID `json:"project_id"`
	CreatedAt time.Time  `json:"created_at"`
	Score     float64    `json:"score"`
}

func (q *Queries) SearchPrompts(ctx context.Context, arg SearchPromptsParams) ([]SearchPromptsRow, error) {
	rows, err := q.db.Query(ctx, searchPrompts,
		arg.Query,
		arg.ProjectIds,
		arg.DateFrom,
		arg.DateTo,
		arg.ResultLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SearchPromptsRow
	for rows.Next() {
		var i SearchPromptsRow
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Body,
			&i.Tags,
			&i.ProjectID,
			&i.CreatedAt,
			&i.Score,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
