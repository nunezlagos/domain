status: in_progress
created: 2026-06-10
implemented: ~
archived: ~
blocked_by: []
soft_dependency:
  - issue-12.6-mcp-tool-resilience-cb-lru-enhancement
rfc: docs/rfc/0006-sdd-pipeline-orchestrator.md
progress: |
  Foundation commit completado en 2026-06-10:
  - Migration 000075_agent_templates_role_seed_managed (role + seed_managed +
    is_user_modified + seed_version + UNIQUE INDEX parcial role='orchestrator')
  - Seeder v3 con 1 sdd-orchestrator + 10 phase-workers (sdd-explore/spec/
    propose/design/tasks/apply/verify/judge/archive/onboard)
  - Cleanup defensivo: borra seed_managed=true que no esté en catálogo actual
    Y sin agent_runs running (respeta is_user_modified=true)
  - Report.Deleted nuevo en internal/seeds/seeds.go
  - Runner.agent marca metadata.standalone=true en direct_invocation
    (preserva compatibilidad con cron orphan-audit issue-08.12)
  - Tests: 5 integration verdes (SeederV3 inserts, UniqueOrchestrator,
    Idempotent xmax, CleanupRemovesLegacy, CleanupPreservesUserModified)
  - Bloqueantes 08.11 + 08.12 implementados y mergeados

  Skeleton commit completado en 2026-06-10 (chunk 2):
  - Runner: RunOption variadic (WithStandalone, WithFlowRun, WithFlowRunStep)
    + buildRunMetadata centralizado + Runner.Env + checkOrphanPolicy
  - INSERT agent_runs ahora persiste flow_run_id explícito + metadata
    derivada de options (vs hardcoded standalone:true)
  - ErrOrphanRunNotAllowed activo: prod sin flow_run_id + standalone=false
    se bloquea proactivo (complementa cron orphan-audit issue-08.12)
  - internal/service/orchestrator/ skeleton:
    * types.go (Mode, PhaseSlug, OrchestrateInput, OrchestrateResult)
    * errors.go (ErrAsyncModeUnsupported D6, ErrRequiredSaveMissing D5,
      ErrInvalidMode, ErrEmptyRawText, ErrUnknownPhase)
    * service.go (Service.Run con validate(): empty, mode válido, D6,
      fases registradas)
    * phases/registry.go (Handler interface + Registry concurrent-safe
      + SuggestedSave + RetryPolicy + MemoryRef + Input/Output/ClientResult)
  - Tests: 21 unit verdes nuevos. Repo 536 tests verdes, cero regresiones.

  Slice Express end-to-end completado en 2026-06-10 (chunk 3):
  - svc-014 phases/sdd_apply.go: code_reference required D5 + RetryCleanup
    + multi_concern detection + blocked-by-client signal
  - svc-015 phases/sdd_verify.go: Gherkin verifier tolerante (Build sin
    apply previo) + RetryReemit + scenarios_failed/blockers wiring
  - svc-003 modes/express.go: BuildExpressPlan dispatcher in-memory
    (sdd-apply → sdd-verify) sin persistencia flow_runs todavía
  - save-001/002 orchestrator/saves.go: ValidateRequiredSaves + tipo
    RequiredSaveError con Unwrap a ErrRequiredSaveMissing
  - Service.Run cuando Mode=Express delega a modes.BuildExpressPlan y
    exporta PhasePlanSummary/PhaseStepSummary/SuggestedSaveSummary para
    desacoplar el subpaquete modes/ del API público
  - Clock inyectable (Clock interface + systemClock + override per test)
  - sab-003 sabotaje verde: apply sin code_reference → ErrRequiredSaveMissing
  - Tests: 38 unit verdes en los 3 paquetes orchestrator + repo total
    564 verdes con -short (+28 vs chunk anterior), cero regresiones

  PENDING (chunks futuros):
  - svc-004..007: modes/{full,solo,detect,async}
  - svc-008: validator.go con DAG-check de SkipPhases
  - svc-009/010/011/012/013/016/017/018: 8 handlers de fase restantes
  - seed-003: flows_catalog.go con flow:sdd-pipeline-v1 DAG
  - save-001..003: SuggestedSave validation post-phase (D5 enforcement)
  - skill-001..003: integración auto-skill (D3 threshold)
  - mcp-001..006: 4 MCP tools nuevos + wire-up + PromptRouter integration
  - cli-001: domain workflow resume <flow_run_id>
  - ana-001..004: Intent analysis mini-pipeline
  - obs-001..004: métricas + OTel spans + alerts
  - test-001..015: 15 escenarios E2E del issue.md
  - sab-001..003: 3 sabotajes
  - doc-001..004: docs/agents + docs/flows + GETTING_STARTED + CHANGELOG
  - state-001..003: implemented + RFC + req.md updates
