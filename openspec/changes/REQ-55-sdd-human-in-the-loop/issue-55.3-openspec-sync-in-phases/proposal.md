# Como plataforma, el sync openspec BD→repo es contrato de fase (propose/design/tasks), no una nota en el protocolo global.

## Why
El sync REQ-55 solo vive en el agent-protocol global; un agente que no lo lee deja openspec/ desincronizado de la BD.

## Scope
Ver REQ-55 proposal + spec.md de este issue.
