status: implemented
created: 2026-06-11
implemented: 2026-06-11
archived: ~
notes: |
  HU-01.10 implementado en 7 commits atomicos sobre
  feat/req-25.5-rls-wiring:

  1. 68022b2 docs(01.10): HU-01.10 spec deployment modes + install/update/restore
  2. 0b04b92 feat(01.10): selector de deployment mode (local/cloud/hybrid) + DSN validation
  3. 39c9801 feat(01.10): backup + restore + AGENTS.md injection marker
  4. 2e79aa5 feat(01.10): wireup CLI install/update/seed/restore
  5. 3167a6f test(01.10): 15 sabotajes que detectan bugs reales + fix ListBackups
  6. 67db272 feat(01.10): wizard visual con InstallProgress + 10 tests
  7. fff386f docs(01.10): seccion install/update/restore en GETTING_STARTED.md

  Cubre los 3 puntos del feedback del usuario:

  1. Selector de deployment mode (local / cloud / hybrid):
     - local: docker compose up -d (Postgres + S3 + SMTP)
     - cloud: pide DSN, valida sslmode, escribe .env
     - hybrid: por servicio (Postgres / S3 / SMTP)

  2. Instalacion NO destructiva:
     - domain init archiva .md a BD (no elimina)
     - flows del user no se tocan (viven en DB)
     - custom .md del user no se tocan (init solo patterns conocidos)
     - AGENTS.md injection con marker '<!-- domain-managed -->'
     - el .md stub sigue existiendo pero es ignorado en practice

  3. Updates con backups automaticos:
     - domain update: backups timestamped antes de mutate
     - seeders idempotentes (skip-by-hash) corren en install Y update
     - domain restore <path>: one-shot recovery de un archivo

  Comandos implementados:
    - domain install [--mode local|cloud|hybrid] (wizard 5 steps con progress)
    - domain update [--no-backup] [--no-seed] [--no-migrate]
    - domain restore <backup-path>
    - domain seed all (expone los seeders idempotentes)

  Verificado: 93 paquetes OK con go test ./... -count=1, 0 FAIL.
  1 bug real detectado y arreglado (ListBackups retornaba nil slice
  en vez de empty slice, rompiendo invariante documentada).

  Pendiente: archivar (se hace en este commit).
