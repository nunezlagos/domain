# Tasks: issue-01.9-first-run-onboard

- [x] **onb-001**: HU spec completo (issue.md, design.md, proposal.md, tasks.md, state.yaml) — 2026-06-11
- [ ] **onb-002**: Endpoint `POST /api/v1/auth/bootstrap` + `GET /api/v1/auth/first-run`
  - service internal/auth/bootstrap con advisory lock
  - handler POST /api/v1/auth/bootstrap (200 si first-run success, 400 si no, 422 si email inválido)
  - handler GET /api/v1/auth/first-run (200 con {is_first_run: bool, user_count: int})
  - tests: first-run, not-first-run, race condition, email inválido
- [ ] **onb-003**: Comando `domain onboard` con TUI minimalista
  - internal/cli/onboard/tui.go (bufio.Scanner)
  - cmd/domain/main.go: nuevo subcommand
  - credentials storage en ~/.config/domain/credentials.json (chmod 600)
  - test E2E con stdin mockeado
- [ ] **onb-004**: Slash command `/domain-login` para opencode + claude-code
  - internal/cli/setup/setup.go: nueva función InstallSlashCommands
  - .opencode/commands/domain-login.md (en ~/.config/opencode/commands/)
  - .claude/commands/domain-login.md (en ~/.claude/commands/)
  - Comando `domain setup opencode` ahora también instala los slash commands
- [ ] **onb-005**: Enforcement de API key al boot del MCP server
  - cmd/domain-mcp/main.go: mejorar mensajes de error con "Run /domain-login"
  - test: API key inválida → exit 1 con mensaje claro
  - test: API key válida → boot normal
- [ ] **onb-006**: Bugfix path detection de `domain-mcp` en `domain setup opencode`
  - internal/cli/setup/targets.go: findDomainMCPSibling con os.Stat
  - test: paths absolutos, symlinks, archivos inexistentes
- [ ] **onb-007**: Test E2E completo del flujo onboard
  - test: fresh install end-to-end con DB real
  - test: ya hay users → OTP flow
  - test: anti-enumeration
  - test: server no responde
  - test: API key sin expires_at sigue funcionando (regression test para "no expira")
- [ ] **onb-008**: Docs `docs/quickstart.md` con flujo paso a paso
  - sección CLI standalone
  - sección slash command /domain-login
  - sección troubleshooting
  - sección security notes (API key leak prevention)
- [ ] **onb-009**: Commit por intención + state.yaml → implemented
  - 6 commits atómicos
  - 0 regresiones en suite
